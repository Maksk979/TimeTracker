<Window x:Class="TimeTracker.App.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление TimeTracker" Height="320" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="#0C0D0F"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Доступно обновление" FontSize="18" FontWeight="SemiBold"
                   Foreground="#F0F0F2" FontFamily="Segoe UI" Margin="0,0,0,16"/>

        <Border Grid.Row="1" Background="#16181C" CornerRadius="8" Padding="16" Margin="0,0,0,16"
                BorderBrush="#2A2D34" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{Binding ReleaseName}" FontSize="14" FontWeight="Medium"
                           Foreground="#F0F0F2" FontFamily="Segoe UI" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding VersionInfo}" FontSize="12"
                           Foreground="#A0A4AC" FontFamily="Segoe UI"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="{Binding StatusText}" FontSize="12"
                   Foreground="#A0A4AC" FontFamily="Segoe UI" Margin="0,0,0,8"/>

        <Grid Grid.Row="3" Height="6" Margin="0,0,0,16">
            <Border Background="#1E2025" CornerRadius="3"/>
            <Border Background="#6C72CB" CornerRadius="3"
                    HorizontalAlignment="Left"
                    Width="{Binding ProgressWidth}"
                    RenderTransformOrigin="0,0.5">
                <Border.RenderTransform>
                    <ScaleTransform ScaleX="{Binding ProgressScale}"/>
                </Border.RenderTransform>
            </Border>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Пропустить" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding SkipCommand}" x:Name="SkipButton"/>
            <Button Content="Обновить" Style="{StaticResource PrimaryButton}"
                    Command="{Binding UpdateCommand}" x:Name="UpdateButton"/>
        </StackPanel>

        <TextBlock Grid.Row="5" Text="{Binding ReleaseNotes}" FontSize="11"
                   Foreground="#72767E" FontFamily="Segoe UI"
                   TextWrapping="Wrap" Margin="0,16,0,0"
                   VerticalAlignment="Top"/>
    </Grid>
</Window>
