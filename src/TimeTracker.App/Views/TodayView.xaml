<UserControl x:Class="TimeTracker.App.Views.TodayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="860">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Сегодня" Style="{StaticResource H1Text}"/>
                <TextBlock Text="{Binding DateLabel}" Style="{StaticResource MonoText}" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="↻ Обновить" Style="{StaticResource PrimaryButton}"
                    Command="{Binding LoadCommand}" VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Активное время" Style="{StaticResource CaptionText}"/>
                    <Border Grid.Row="2" Background="{StaticResource GoodBrush}" CornerRadius="2" Height="3" HorizontalAlignment="Left" Width="40"/>
                    <TextBlock Grid.Row="2" Text="{Binding ActiveTime}" Style="{StaticResource MetricValue}" Margin="0,8,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorder}" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Простой" Style="{StaticResource CaptionText}"/>
                    <Border Grid.Row="2" Background="{StaticResource WarnBrush}" CornerRadius="2" Height="3" HorizontalAlignment="Left" Width="40"/>
                    <TextBlock Grid.Row="2" Text="{Binding IdleTime}" Style="{StaticResource MetricValue}" Margin="0,8,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource CardBorder}" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Переключений" Style="{StaticResource CaptionText}"/>
                    <Border Grid.Row="2" Background="{StaticResource AccentBrush}" CornerRadius="2" Height="3" HorizontalAlignment="Left" Width="40"/>
                    <TextBlock Grid.Row="2" Text="{Binding SwitchCount}" Style="{StaticResource MetricValue}" Margin="0,8,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Сессии" Style="{StaticResource H2Text}" Margin="0,0,0,12"/>
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Sessions}"
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ВРЕМЯ" Binding="{Binding StartedAtLocal, StringFormat=HH:mm}" Width="70"/>
                        <DataGridTextColumn Header="ПРОЦЕСС" Binding="{Binding ProcessName}" Width="140"/>
                        <DataGridTextColumn Header="КАТЕГОРИЯ" Binding="{Binding Category.Name}" Width="120"/>
                        <DataGridTextColumn Header="ЗАГОЛОВОК" Binding="{Binding WindowTitle}" Width="*"/>
                        <DataGridTextColumn Header="ДЛИТ." Binding="{Binding DurationFormatted}" Width="70"/>
                        <DataGridCheckBoxColumn Header="IDLE" Binding="{Binding IsIdle}" Width="50" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
