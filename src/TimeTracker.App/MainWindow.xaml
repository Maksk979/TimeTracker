<Window x:Class="TimeTracker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:TimeTracker.App.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TimeTracker" Height="640" Width="900"
        MinHeight="560" MinWidth="760"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SurfaceBrush}"
        StateChanged="Window_StateChanged">
    <TabControl Style="{StaticResource DarkTabControl}" Margin="0">
        <TabItem Header="Сегодня" Style="{StaticResource DarkTabItem}">
            <views:TodayView x:Name="TodayTab"/>
        </TabItem>
        <TabItem Header="Графики" Style="{StaticResource DarkTabItem}">
            <views:ChartsView x:Name="ChartsTab"/>
        </TabItem>
        <TabItem Header="Правила" Style="{StaticResource DarkTabItem}">
            <views:RulesView x:Name="RulesTab"/>
        </TabItem>
        <TabItem Header="Настройки" Style="{StaticResource DarkTabItem}">
            <views:SettingsView x:Name="SettingsTab"/>
        </TabItem>
        <TabItem Header="Отчёты" Style="{StaticResource DarkTabItem}">
            <views:ReportsView x:Name="ReportsTab"/>
        </TabItem>
    </TabControl>
</Window>
